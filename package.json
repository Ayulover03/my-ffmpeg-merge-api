{
  "name": "ffmpeg-merge-api",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "echo 'No build needed'"
  },
  "dependencies": {
    "@ffmpeg/ffmpeg": "^0.11.6",
    "@ffmpeg/util": "^0.0.7",
    "node-fetch": "^3.3.2"
  }
}
